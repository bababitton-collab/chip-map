# What chains actually imports. Pinned to a minor line so a weekly CI build
# does not change its own dependencies underneath it.
#
# polars   the whole price and relative-return layer; there is no pandas here
# pyarrow  polars' parquet reader/writer, an explicit dependency because the
#          price store IS parquet and a missing engine fails at write time
# httpx    the EODHD client, the EDGAR client and the answers fetch
# tenacity the EODHD retry policy
polars~=1.44
pyarrow~=25.0
httpx~=0.28
tenacity~=9.1

# tests only
pytest~=8.3
respx~=0.23
